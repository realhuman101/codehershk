import React, { useRef } from 'react'
import Image from 'next/image';
import { useScroll, useTransform, motion} from 'framer-motion';

function ImageParallax() {
  return (
	<div>ImageParallax</div>
  )
}

export default ImageParallax