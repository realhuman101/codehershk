/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  assetPrefix: '/codehershk/',
  basePath: '/codehershk',
}

module.exports = nextConfig

export default nextConfig;
